<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Só rede. Nada de câmera/localização — caixa puro. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- tools:replace=supportsRtl: o AAR do SDK Cielo declara true; mantemos false. -->
    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/Theme.ConciliaGarcom"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="false"
        tools:replace="android:supportsRtl">

        <!-- Exigido pela Cielo Store: metadado de integração. Usamos o Order
             Manager SDK (pagamento local NO terminal, exigência da certificação
             pra apps transacionais) — integração tipo "sdk".
             tools:replace: o AAR do SDK também declara este metadado. -->
        <meta-data
            android:name="cs_integration_type"
            android:value="sdk"
            tools:replace="android:value" />

        <!-- Entrada: roteia pra login ou mesas conforme a sessão. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".MesasActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".ContaActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Entregas: rota (saí/cheguei/entregue) e receber na porta pela maquininha. -->
        <activity
            android:name=".EntregasActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Lançar itens do cardápio na mesa (busca + categorias + quantidade). -->
        <activity
            android:name=".ProdutosActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />
    </application>
</manifest>
